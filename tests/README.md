# Verification

Run the commands in [README](../README.md). Current tests cover the health/schema contract, unknown routes, absence of web-stack imports from the library root, and static inward dependency rules including relative imports.

Add domain/use-case, provider integration, data migration and research-journey tests with the corresponding behavior. Static checks do not prove runtime-import or cross-capability correctness.
