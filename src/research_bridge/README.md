# research_bridge

Application source root for one installable distribution. The top-level import exposes a version without importing FastAPI or starting services. Bootstrap assembles the process-health API; business capabilities remain scaffolds. See [architecture](../../docs/architecture.md).
